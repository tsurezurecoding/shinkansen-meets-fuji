{
  "name": "mado-app",
  "private": true,
  "version": "0.0.0",
  "description": "新幹線の窓 (michikusa-travel.com) — static site build and validation entry points.",
  "//type": "Intentionally NOT \"module\". app/*.js are browser scripts loaded by the generators via vm.runInNewContext (sloppy-mode script semantics); leaving the default keeps `node --check` faithful to that. The build/validate scripts are .mjs and are ESM regardless.",
  "scripts": {
    "build": "npm run build:data && npm run sync:versions && npm run fix:spot-counts && npm run build:jrpass && npm run build:pages",
    "build:data": "node scripts/generate-spot-page-shared-data.mjs",
    "build:pages": "node scripts/generate-spot-pages.mjs",
    "sync:versions": "node scripts/sync-asset-versions.mjs",
    "preflight:spots": "node scripts/generate-spot-page-shared-data.mjs --check && node scripts/generate-spot-pages.mjs --check",
    "check": "npm run check:syntax && npm run check:content && npm run check:timetable && npm run check:train-select && npm run check:spot-pages && npm run check:language && npm run check:ui-strings && npm run check:727 && npm run check:sparkling && npm run check:jrpass && npm run check:spot-counts && npm run check:versions",
    "check:syntax": "node --check data.js && node --check app.js && node --check track.js && node --check spot-page-shared.js && node --check data/timetable.js && node --check live/live.js && node --check train-select.js",
    "check:content": "node scripts/validate-content.mjs",
    "check:timetable": "node scripts/validate-timetable.mjs",
    "check:train-select": "node scripts/validate-train-select.mjs",
    "check:spot-pages": "node scripts/validate-spot-page-shared.mjs",
    "check:language": "node scripts/validate-language-pages.mjs",
    "check:ui-strings": "node scripts/validate-ui-strings.mjs",
    "check:727": "node scripts/validate-727-collection.mjs",
    "check:sparkling": "node scripts/validate-sparkling-dreams.mjs",
    "check:versions": "node scripts/sync-asset-versions.mjs --check",
    "audit:site": "node scripts/audit-site.mjs",
    "serve": "py -m http.server 8137",
    "check:spot-counts": "node scripts/validate-spot-counts.mjs",
    "fix:spot-counts": "node scripts/validate-spot-counts.mjs --fix",
    "build:jrpass": "node scripts/generate-jr-pass-table.mjs",
    "check:jrpass": "node scripts/generate-jr-pass-table.mjs --check"
  }
}
